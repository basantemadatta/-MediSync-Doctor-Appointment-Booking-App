name: bmi_calc
description: A new Flutter project.
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ^3.5.0

dependencies:
  flutter:
    sdk: flutter
  http: ^1.2.2
  firebase_core: ^3.9.0
  firebase_auth: ^5.3.4
  cupertino_icons: ^1.0.2
  cloud_firestore: ^5.6.0
  shared_preferences: ^2.0.18 # Moved here


dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:

  uses-material-design: true
